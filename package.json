{
  "name": "suhaibjanjua.github.io",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "npm run prebuild && nuxt build && npm run postbuild",
    "prebuild": "export NODE_OPTIONS=--openssl-legacy-provider",
    "postbuild": "mv dist docs && cd docs && rm README.md",
    "start": "nuxt start",
    "generate": "nuxt generate"
  },
  "dependencies": {
    "@nuxt/content": "^1.9.0",
    "@nuxtjs/axios": "^5.12.2",
    "@nuxtjs/pwa": "^3.0.2",
    "core-js": "^3.6.5",
    "nuxt": "^2.14.6",
    "vue-burger-menu": "^2.0.5",
    "vue-feather-icons": "^5.1.0",
    "vue-sequential-entrance": "^1.1.3"
  },
  "devDependencies": {
    "@nuxtjs/tailwindcss": "^3.1.0"
  }
}